# Agent instructions

このリポジトリを扱うモデルは、最初に`docs/HANDOFF.md`、`docs/REBUILD_SPEC.md`、`docs/TEST_PLAN.md`を全文読んでください。

## 絶対要件

- 個別Upgrade名への例外追加だけで不具合を閉じない。
- 価格の大小を効率の代用にしない。
- 施設、Upgrade、待機を同じ経済指標で比較する。
- 自動クリックによる収益を全評価経路へ含める。
- 「待つ」を常に正式な行動候補として扱う。
- 予約対象が購入可能になった周期に別施設へフォールバックしない。
- 実画面の判断値をエクスポート可能にし、報告された状態をオフラインで再生できるようにする。
- Tampermonkey多重起動を防止する。
- 日本語表示に依存せず、安定した内部IDとGameオブジェクトを使う。

## 作業手順

1. 不具合報告時は、コードを変更する前に診断スナップショットで候補生成、効果測定、順位付け、購入実行のどこが壊れたか特定する。
2. 失敗を再現するfixtureを追加する。
3. 中核ロジックを修正する。
4. 同種の建物・Upgradeへ一般化したテストを追加する。
5. JavaScript構文、純粋関数テスト、Gameモック統合テスト、実画面スモークテストを実行する。
6. 設計書とHANDOFFの既知課題を更新する。

## 禁止事項

- 「最大待機時間を伸ばす」だけの修正。
- 最安施設または最安Upgradeを通常判断として購入する処理。
- `priority`や`critical`分類を候補参加条件にすること。
- 候補購入が失敗した直後、同じ周期に無関係な候補を買うこと。
- 実画面未確認なのに「修正済み」と断定すること。


